terraform {
  backend "azurerm" {
    # Estos valores deben ser provistos externamente (vía CLI o variables de entorno en CI/CD)
    # resource_group_name  = "utbgo-tfstate-rg"
    # storage_account_name = "utbgotfstate"
    # container_name       = "tfstate"
    # key                  = "terraform.tfstate"
  }
}
